<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
</head>
<body>
    <h1>This is Edit Page</h1>
    <form action="/editUser" method="post">
        <table>
            <tr>
                <td>
                    <label for="editId">ID</label>
                    <input type="number" name="editId" value="<%= editData.userid %>" readonly>
                </td>
            </tr>
            <tr>
                <td>
                    <label for="name">Name</label>
                    <input type="text" name="name" value="<%= editData.name %>" placeholder="userName">
                </td>
            </tr>
            <tr>
                <td>
                    <label for="email">Email</label>
                    <input type="email" name="email" value="<%= editData.email %>" placeholder="userEmail">
                </td>
            </tr>
            <tr>
                <td>
                    <label for="password">Password</label>
                    <input type="password" name="password" value="<%= editData.password %>" placeholder="userPassword">
                </td>
            </tr>
            <tr>
                <td>
                    <input type="submit" value="Submit">
                </td>
            </tr>
        </table>
    </form>
</body>
</html>
